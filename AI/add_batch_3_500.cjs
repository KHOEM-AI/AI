const fs = require("fs");

const path = "src/ai/khmer.mjs";
const text = fs.readFileSync(path, "utf8");

function normalize(s) {
  return s.normalize("NFC").trim().toLowerCase().replace(/\s+/g, " ");
}

function escapeJs(s) {
  return String(s)
    .replace(/\\/g, "\\\\")
    .replace(/"/g, '\\"')
    .replace(/\r?\n/g, "\\n");
}

// ============================================================
// Batch 3 subjects
// ============================================================

const subjects = [
  ["គ្រួសារ", "គ្រួសារ"],
  ["ឪពុកម្តាយ", "ឪពុកម្តាយ"],
  ["បងប្អូន", "បងប្អូន"],
  ["មិត្តភក្តិ", "មិត្តភក្តិ"],
  ["ការងារ", "ការងារ"],
  ["ថៅកែ", "ថៅកែ"],
  ["មិត្តរួមការងារ", "មិត្តរួមការងារ"],
  ["សាលា", "សាលា"],
  ["គ្រូ", "គ្រូ"],
  ["មេរៀន", "មេរៀន"],
  ["ការប្រឡង", "ការប្រឡង"],
  ["សៀវភៅ", "សៀវភៅ"],
  ["ការងារផ្ទះ", "ការងារផ្ទះ"],
  ["ហាត់ប្រាណ", "ការហាត់ប្រាណ"],
  ["សុខភាព", "សុខភាព"],
  ["ការគេង", "ការគេង"],
  ["ការសម្រាក", "ការសម្រាក"],
  ["ផ្ទះ", "ផ្ទះ"],
  ["បន្ទប់", "បន្ទប់"],
  ["ផ្ទះបាយ", "ផ្ទះបាយ"],
  ["អាហារ", "អាហារ"],
  ["ភេសជ្ជៈ", "ភេសជ្ជៈ"],
  ["កាហ្វេ", "កាហ្វេ"],
  ["តែ", "តែ"],
  ["ទឹក", "ទឹក"],
  ["ផ្សារ", "ផ្សារ"],
  ["ហាង", "ហាង"],
  ["ការទិញទំនិញ", "ការទិញទំនិញ"],
  ["ប្រាក់", "ប្រាក់"],
  ["ថវិកា", "ថវិកា"],
  ["ការសន្សំ", "ការសន្សំ"],
  ["ធនាគារ", "ធនាគារ"],
  ["ទូរស័ព្ទ", "ទូរស័ព្ទ"],
  ["អ៊ីនធឺណិត", "អ៊ីនធឺណិត"],
  ["WiFi", "WiFi"],
  ["កុំព្យូទ័រ", "កុំព្យូទ័រ"],
  ["កម្មវិធី", "កម្មវិធី"],
  ["website", "website"],
  ["coding", "coding"],
  ["JavaScript", "JavaScript"],
  ["TypeScript", "TypeScript"],
  ["Python", "Python"],
  ["Git", "Git"],
  ["GitHub", "GitHub"],
  ["ការធ្វើដំណើរ", "ការធ្វើដំណើរ"],
  ["ឡាន", "ឡាន"],
  ["ម៉ូតូ", "ម៉ូតូ"],
  ["ឡានក្រុង", "ឡានក្រុង"],
  ["ផែនការ", "ផែនការ"],
  ["ពេលវេលា", "ពេលវេលា"],
  ["អនាគត", "អនាគត"],
  ["គោលដៅ", "គោលដៅ"],
  ["បទពិសោធន៍", "បទពិសោធន៍"],
  ["ជំនាញ", "ជំនាញ"],
  ["ការងារ", "ការងារ"],
  ["ការសិក្សា", "ការសិក្សា"],
  ["ការអប់រំ", "ការអប់រំ"],
  ["ទំនាក់ទំនង", "ទំនាក់ទំនង"],
  ["បច្ចេកវិទ្យា", "បច្ចេកវិទ្យា"],
  ["សុខភាព", "សុខភាព"],
  ["ជីវិត", "ជីវិត"],
  ["ទម្លាប់", "ទម្លាប់"]
];

// ============================================================
// 10 templates × 50 subjects = 500 candidates
// ============================================================

const templates = [
  {
    q: s => `ចង់ដឹងបន្ថែមអំពី ${s[0]}`,
    r: s => `បាន។ ខ្ញុំអាចជួយពន្យល់អំពី${s[1]} តាមព័ត៌មានដែលបងផ្តល់។`
  },
  {
    q: s => `តើ ${s[0]} មានសារៈសំខាន់យ៉ាងម៉េច`,
    r: s => `${s[1]} អាចមានសារៈសំខាន់ខុសគ្នាតាមស្ថានភាព និងគោលដៅរបស់បង។`
  },
  {
    q: s => `តើគួររៀបចំ ${s[0]} យ៉ាងដូចម្តេច`,
    r: s => `សាករៀបចំ${s[1]} តាមអាទិភាព ហើយធ្វើម្តងមួយជំហាន។`
  },
  {
    q: s => `ចង់កែលម្អ ${s[0]}`,
    r: s => `អាចកំណត់ចំណុចដែលចង់កែលម្អក្នុង${s[1]} ហើយអនុវត្តបន្តិចម្តងៗ។`
  },
  {
    q: s => `តើមានវិធីងាយសម្រាប់ ${s[0]} ទេ`,
    r: s => `អាចចាប់ផ្តើមពីវិធីសាមញ្ញបំផុត ហើយបន្ថែមជំហាននៅពេលចាំបាច់។`
  },
  {
    q: s => `ខ្ញុំមានបញ្ហាជាមួយ ${s[0]}`,
    r: s => `ប្រាប់ខ្ញុំថាបញ្ហាជាមួយ${s[1]} ជាអ្វី ហើយខ្ញុំនឹងជួយរកជំហានដោះស្រាយ។`
  },
  {
    q: s => `មិនទាន់យល់អំពី ${s[0]}`,
    r: s => `មិនអីទេ។ អាចពន្យល់បន្ថែមអំពី${s[1]} ជាជំហានៗបាន។`
  },
  {
    q: s => `ចង់ចាប់ផ្តើម ${s[0]}`,
    r: s => `ចាប់ផ្តើមពីមូលដ្ឋាននៃ${s[1]} ហើយកុំដាក់ការងារច្រើនពេកក្នុងពេលតែមួយ។`
  },
  {
    q: s => `តើអាចជួយខ្ញុំជាមួយ ${s[0]} បានទេ`,
    r: s => `បាន។ ផ្ញើព័ត៌មាន ឬបញ្ហាដែលពាក់ព័ន្ធនឹង${s[1]} មក ខ្ញុំនឹងជួយពិនិត្យ។`
  },
  {
    q: s => `តើគួរចាប់ផ្តើមពីណាសម្រាប់ ${s[0]}`,
    r: s => `សាកចាប់ផ្តើមពីគោលដៅច្បាស់មួយដែលទាក់ទងនឹង${s[1]} រួចបំបែកជាជំហានតូចៗ។`
  }
];

const generated = [];

for (const template of templates) {
  for (const subject of subjects) {
    generated.push([
      template.q(subject),
      template.r(subject)
    ]);
  }
}

// ============================================================
// Existing keys
// ============================================================

const existing = new Set();

for (const line of text.split("\n")) {
  const m = line.match(/^\s*\["([^"]*)",/);
  if (m) existing.add(normalize(m[1]));
}

// ============================================================
// Deduplicate
// ============================================================

const unique = [];
const batchSeen = new Set();

for (const [key, response] of generated) {
  const k = normalize(key);

  if (existing.has(k)) continue;
  if (batchSeen.has(k)) continue;

  batchSeen.add(k);
  unique.push([key, response]);

  if (unique.length === 500) break;
}

console.log(`📦 Existing entries: ${existing.size}`);
console.log(`🧩 Generated candidates: ${generated.length}`);
console.log(`➕ Unique new entries: ${unique.length}`);

if (unique.length < 500) {
  console.error("❌ Could not generate 500 unique entries.");
  process.exit(1);
}

// ============================================================
// Backup
// ============================================================

const stamp = new Date().toISOString().replace(/[:.]/g, "-");
const backup = `${path}.bak-add500-batch3-${stamp}`;

fs.copyFileSync(path, backup);

// ============================================================
// Insert before final ]; 
// ============================================================

const pos = text.lastIndexOf("];");

if (pos === -1) {
  console.error("❌ Could not find final ];");
  process.exit(1);
}

const block =
  "\n\n  // ============================================================\n" +
  "  // Expansion Batch 3 — 500 entries\n" +
  "  // ============================================================\n" +
  unique
    .map(([key, response]) =>
      `  ["${escapeJs(key)}", "${escapeJs(response)}"],`
    )
    .join("\n") +
  "\n";

const result = text.slice(0, pos) + block + text.slice(pos);

fs.writeFileSync(path, result, "utf8");

console.log(`💾 Backup: ${backup}`);
console.log(`✅ Added: ${unique.length}`);
console.log(`📁 Updated: ${path}`);
